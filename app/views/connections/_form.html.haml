= form_for(@connection) do |f|
  - if @connection.errors.any?
    #error_explanation
      %h2
        = pluralize(@connection.errors.count, "error")
        prohibited this connection from being saved:
      %ul
        - @connection.errors.full_messages.each do |msg|
          %li= msg

  =f.hidden_field :trainer_id, {value: @connection.trainer_id}
  =f.hidden_field :trainee_id, {value: @connection.trainee_id}
  .actions
    = f.submit